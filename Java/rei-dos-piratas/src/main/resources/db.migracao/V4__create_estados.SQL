-- V4: Criação da tabela ESTADOS
CREATE TABLE ESTADOS (
    id           NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    estado_nome  VARCHAR2(30) NOT NULL,
    estado_sigla VARCHAR2(2)  NOT NULL
);

